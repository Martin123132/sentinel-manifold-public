# v0.1.6 Suite Catalog Verification

This checklist records the verification used to publish
`v0.1.6 Suite Catalog`.

Release:

- GitHub Release: https://github.com/Martin123132/sentinel-manifold-public/releases/tag/v0.1.6
- Tag: `v0.1.6`
- Label: `v0.1.6 Suite Catalog`
- Live sandbox: https://sentinel-manifold-public.onrender.com/

## Release Scope

- [x] Add `docs/suite-catalog.md` as the buyer starter-pack guide.
- [x] Add five runnable catalog suites under `samples/catalog/`.
- [x] Cover support operations, regulated approval, research claims,
  code-review release notes, and agent/tool boundaries.
- [x] Add tests proving every catalog suite validates and passes.
- [x] Update CI to validate the catalog and run every catalog suite.
- [x] Upload `out/catalog-*-suite-report.json` in the release-gate artifact.

## Runtime Boundary

- [x] No API changes.
- [x] No CLI behavior changes.
- [x] No dashboard/runtime changes.
- [x] No provider keys or private evidence are added.
- [x] Public sandbox behavior remains unchanged.

## Verification

```powershell
python -m unittest discover -s tests
python -m compileall app
python -m py_compile scripts\build-evidence-example.py
python -m py_compile scripts\run-proof-pack.py
python -m py_compile scripts\validate-suite.py
node --check web\app.js
python scripts\validate-suite.py --run samples\catalog\*.json
python scripts\run-proof-pack.py
python scripts\run-proof-pack.py --full
powershell -ExecutionPolicy Bypass -File .\scripts\run-proof-pack.ps1
```

## Publish Checks

- [x] Latest `main` CI is green before release.
- [x] `CHANGELOG.md` has a dated `v0.1.6` section.
- [x] `RELEASE_NOTES.md` is final, not draft wording.
- [x] No generated `out/` artifacts are committed.
- [x] GitHub Release is published as latest, non-draft, non-prerelease.
