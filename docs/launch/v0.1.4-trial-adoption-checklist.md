# v0.1.4 Trial Adoption Checklist

Use this before publishing `v0.1.4 Trial Adoption`.

This release is not published yet.

## Release Scope

- [ ] `TRIAL_GUIDE.md` gives a clear 10-minute trial path.
- [ ] `scripts/run-proof-pack.ps1` runs the public proof pack locally.
- [ ] GitHub issue templates capture trial reports, false positives, missed
  drift, and commercial enquiries.
- [ ] `docs/trial-feedback-template.md` gives testers a safe feedback shape.
- [ ] README points trial users to the guide and proof-pack script.
- [ ] `RELEASE_NOTES.md` is ready for `v0.1.4 Trial Adoption`.

## Runtime Boundary

- [ ] No API changes.
- [ ] No CLI behavior changes.
- [ ] No dashboard/runtime changes.
- [ ] No provider keys or private evidence are added.
- [ ] Public sandbox behavior remains unchanged.

## Verification

```powershell
python -m unittest discover -s tests
python -m compileall app
python -m py_compile scripts\build-evidence-example.py
node --check web/app.js
powershell -ExecutionPolicy Bypass -File .\scripts\run-proof-pack.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\run-proof-pack.ps1 -Full
```

## Publish Checks

- [ ] Latest `main` CI is green.
- [ ] `CHANGELOG.md` has a dated `v0.1.4 Trial Adoption` section.
- [ ] No generated `out/` artifacts are committed.
- [ ] GitHub Release is published as latest, non-draft, non-prerelease.
