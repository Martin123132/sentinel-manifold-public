# v0.1.3 Release Verification Checklist

This checklist records the verification work completed before publishing
`v0.1.3 Proof Candidate`.

Release:

- GitHub Release: https://github.com/Martin123132/sentinel-manifold-public/releases/tag/v0.1.3
- Tag: `v0.1.3`
- Target commit: `61ec52cade15e939309a22205718d273b73d6662`
- CI run: https://github.com/Martin123132/sentinel-manifold-public/actions/runs/26881961975

## Documentation Alignment

- [x] `README.md` points at live `v0.1.3` and keeps previous release links visible.
- [x] `ROADMAP.md` shows `v0.1.3` as the current release milestone.
- [x] `PRODUCT_BRIEF.md` includes `Stage 30` and the shipped proof statement.
- [x] `CHANGELOG.md` contains a dated `v0.1.3 Proof Candidate` release section.
- [x] `RELEASE_NOTES.md` contains the published `v0.1.3` release body.
- [x] `docs/launch/public-launch-copy.md` reflects the live `v0.1.3` release.
- [x] `docs/launch/post-release-checklist.md` has the `v0.1.3` release metadata.
- [x] `docs/proof-gallery/README.md` includes and links to
  `docs/proof-gallery/customer-operations-conversion.md`.
- [x] New proof walkthrough file exists and renders:
  `docs/proof-gallery/customer-operations-conversion.md`.

## Runtime and Demo Sanity

- [x] `/api/health` returns `public_demo: true` on the hosted public sandbox.
- [x] `/api/providers` returns only `local_demo` for unauthenticated callers.
- [x] `/api/audits` returns `401` for unauthenticated callers.
- [x] Public suite run still returns PASS with `5` cases, `5` passed, `0` failed.
- [x] `/api/demo-suite` + `/api/suite` still match the current public route behavior.

## Evidence and Suite Validation

- [x] `docs/proof-gallery/customer-operations-conversion.md` references
  `samples/customer-shaped-regression-suite.json` case IDs accurately.
- [x] `python app/cli.py suite --input samples/customer-shaped-regression-suite.json --out out/customer-shaped-regression-suite-report.json --fail-on-fail`
  passes.
- [x] Existing suite commands pass for the release gate pack (regression, buyer depth,
  calibration, tuning, mixed proof, agent policy, integration, external adoption).

## Release Hygiene

- [x] `main` is clean and green in CI.
- [x] CI artifact names remain stable (`sentinel-release-gate`).
- [x] No untracked generated artifacts (`out/*`) are committed.
- [x] No runtime/API/CLI behavior changes were made in this stage.
