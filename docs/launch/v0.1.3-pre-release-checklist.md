# v0.1.3 Pre-Release Checklist

Use this checklist before publishing `v0.1.3 Proof Candidate`.

## Documentation Alignment

- [ ] `README.md` says `v0.1.2` is live and clearly marks `v0.1.3` as candidate.
- [ ] `ROADMAP.md` shows the `v0.1.3` candidate as the next release milestone.
- [ ] `PRODUCT_BRIEF.md` includes `Stage 30` and the candidate statement.
- [ ] `CHANGELOG.md` top `Unreleased` contains the candidate prep notes.
- [ ] `RELEASE_NOTES.md` is set to draft body for `v0.1.3` and does not claim
  published release status.
- [ ] `docs/launch/public-launch-copy.md` reflects both live `v0.1.2` and prepared
  `v0.1.3` state.
- [ ] `docs/launch/post-release-checklist.md` has a `v0.1.3` prep section and links
  to this file.
- [ ] `docs/proof-gallery/README.md` includes and links to
  `docs/proof-gallery/customer-operations-conversion.md`.
- [ ] New proof walkthrough file exists and renders:
  `docs/proof-gallery/customer-operations-conversion.md`.

## Runtime and Demo Sanity

- [ ] `/api/health` returns `public_demo: true` on the hosted public sandbox.
- [ ] `/api/providers` returns only `local_demo` for unauthenticated callers.
- [ ] `/api/audits` returns `401` for unauthenticated callers.
- [ ] Public suite run still returns PASS with `5` cases, `5` passed, `0` failed.
- [ ] `/api/demo-suite` + `/api/suite` still match the current public route behavior.

## Evidence and Suite Validation

- [ ] `docs/proof-gallery/customer-operations-conversion.md` references
  `samples/customer-shaped-regression-suite.json` case IDs accurately.
- [ ] `python app/cli.py suite --input samples/customer-shaped-regression-suite.json --out out/customer-shaped-regression-suite-report.json --fail-on-fail`
  passes.
- [ ] Existing suite commands pass for the release gate pack (regression, buyer depth,
  calibration, tuning, mixed proof, agent policy, integration, external adoption).

## Release Hygiene

- [ ] `main` is clean and cleanly green in CI.
- [ ] CI artifact names, if changed, remain stable (`sentinel-release-gate`).
- [ ] No untracked generated artifacts (`out/*`) are committed.
- [ ] No runtime/API/CLI behavior changes were made in this stage.
